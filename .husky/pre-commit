#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged

# Note: type-check désactivé temporairement car il y a encore des erreurs TS non-critiques
# À réactiver une fois les erreurs TS corrigées: npm run type-check
